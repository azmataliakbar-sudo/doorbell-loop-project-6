# doorbell

Project 6 from the Loop Engineering crash course.

## Planted bug

`src/calc.js` has `multiply` returning `a - b` instead of `a * b`.

## The loop

GitHub Actions fires on `pull_request` and runs `reviewer.js` automatically.
No prompt typed.
